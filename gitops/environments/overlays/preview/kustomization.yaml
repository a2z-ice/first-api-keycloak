apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NOTE: namespace, image tags, APP_URL, KEYCLOAK_CLIENT_ID, and Ingress host
# are all injected by the ArgoCD ApplicationSet PullRequest Generator template
# via kustomize.namespace, kustomize.images, and kustomize.patches.
# No per-PR files are committed here — ArgoCD parameterizes in-memory.

namespace: student-app-preview

resources:
  - ../../base
  - namespace.yaml
